export interface EventT{
    title: string;
    location?: string;
    message: string;
    startDate: Date;
    starDay: string;
    starMonth: string;
    starYear: string;
    endDate?: Date;
    timeC: number;
}